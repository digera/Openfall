package main

// Occupancy is replicated as column heights on GameState (all seven) and on
// the 30 Hz snapshot (up to two dirty pylons). The bite-reproduction and
// PylonSync protocol that used to live here is gone.
